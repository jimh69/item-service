spring:
  application:
    name: item-service
  main:
    allow-bean-definition-overriding: true

# Logging for bootstrap configuration
logging:
  level:
    org.springframework.cloud: INFO
    org.springframework.boot.context.config: INFO
